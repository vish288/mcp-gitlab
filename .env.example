# GitLab instance URL (required)
GITLAB_URL=https://gitlab.example.com

# Authentication token (required) — any of these work:
# - Personal access token (glpat-xxx) with api scope
# - OAuth2 token
# - CI job token ($CI_JOB_TOKEN)
GITLAB_TOKEN=glpat-xxxxxxxxxxxxxxxxxxxx
# Also accepts: GITLAB_PAT

# Optional settings
# GITLAB_READ_ONLY=false
# GITLAB_TIMEOUT=30
# GITLAB_SSL_VERIFY=true
